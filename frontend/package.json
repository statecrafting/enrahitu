{
  "name": "@enrahitu/frontend",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "spec-spine": { "spec": "006-webapp-spa" },
  "type": "module",
  "description": "Minimal enrahitu SPA: login (mock + rauthy), profile, logout, cache demo. Builds into backend/web/dist, served by the backend's static service.",
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.0",
    "typescript": "^6.0.3",
    "vite": "^7.0.0"
  }
}
