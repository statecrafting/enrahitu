# Rust build output (the linux cross-build uses target-linux, see docker/)
/target
/target-linux
/node_modules
Cargo.lock
# napi build artifacts: the platform .node binary and generated JS loader.
# Regenerated by `npm run build` (see RESULTS.md lineage in docs/ARCHITECTURE.md).
*.node
index.js
index.d.ts
*.tgz
